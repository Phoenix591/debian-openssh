Tests:
 regress,
Restrictions:
 allow-stderr,
 isolation-container,
 needs-root,
Depends:
 devscripts,
 dropbear,
 haveged,
 openssh-tests,
 openssl,
 putty-tools,
 python3-twisted,
 sudo,
 sysvinit-utils,

Tests:
 ssh-gssapi,
Restrictions:
 allow-stderr,
 isolation-container,
 needs-root,
Depends:
 krb5-admin-server,
 krb5-kdc,
 openssh-server,

Tests:
 socket-activation,
Restrictions:
 allow-stderr,
 isolation-container,
 needs-root,
Depends:
 openssh-server,
